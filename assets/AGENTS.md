# Documentation Manager

At feature close, explicitly invoke `docmanager document-change` with one selected scope. Review the evidence and apply any documentation edits yourself; this tool never writes documentation.
